import { MaximumProfit } from './maximum-profit';


describe('MaximumProfit', (): void => {
    test('should say hello to Tom.', (): void => {
        const s = new MaximumProfit()
        expect(s).toBeTruthy();
    });
})
